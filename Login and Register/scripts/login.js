document.getElementById("loginForm").addEventListener("submit", function(event) {
    event.preventDefault(); // Previne trimiterea formularului
    // Redirectează la pagina principală
    window.location.href = "main.html";
});